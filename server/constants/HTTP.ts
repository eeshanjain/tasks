export const HTTP_SERVER_ERROR = parseInt(JSON.stringify(process.env.HTTP_SERVER_ERROR), 10) || 500
export const HTTP_OK = parseInt(JSON.stringify(process.env.HTTP_OK), 10) || 200
export const HTTP_CREATED = parseInt(JSON.stringify(process.env.HTTP_CREATED), 10) || 201
